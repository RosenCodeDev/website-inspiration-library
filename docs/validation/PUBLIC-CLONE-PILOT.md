# Public Clone Pilot

Last verified: 2026-08-24

Command: `npm run test:public-clone`

The network-dependent pilot reconstructed the approved Aside hero through the installed Design Taste Injection skill, localized its DOM, CSS, fonts, and images, and produced an identity-safe Aster remix in an independent temporary project.

Verified results:

- No external requests, missing local assets, hotlinks, source scripts, or analytics.
- Responsive unmasked difference: 0.13% at 1440 px, 0.52% at 768 px, and 0.85% at 390 px. The automatic pass limit is 5%.
- Local interaction, workbench registration, state validation, identity removal, and the production build passed.
- The source clone received a full-size visual review.

This result proves the curated public pilot, not arbitrary-site cloning. The live pilot remains outside CI because public uptime, site changes, and bot protection are nondeterministic. CI uses the controlled self-contained clone fixture instead.
